#include "main.h"

/**
 * main - Test function for positive or negative
 * Return: 0
 */
int main(void)
{
    int i = 0; // Initialize and assign a value to 'i'
    positive_or_negative(i);
    return (0); // Use a semicolon to end the statement, not a colon (:)
}

